# Архитектура API для сервиса тестирования прокси

## Дата: 30 октября 2025 года

## 🎯 Цель
Создать REST API для управления процессом тестирования прокси, получения результатов и мониторинга состояния системы.

## 📋 Требования к API

### Основные функции:
1. **Управление тестированием:** Запуск/остановка тестов
2. **Конфигурация:** Настройка параметров тестирования
3. **Результаты:** Получение статистики и списков прокси
4. **Мониторинг:** Статус системы и метрики в реальном времени
5. **История:** Хранение результатов предыдущих тестов

### Технические требования:
- **RESTful API** с JSON форматом
- **Асинхронная обработка** длительных операций
- **Аутентификация** и авторизация
- **Документация** OpenAPI/Swagger
- **Логирование** и мониторинг

## 🏗️ Предлагаемая архитектура

### Вариант 1: Go с Gin/Gorilla Mux
**Преимущества:**
- Нативный для текущего проекта
- Высокая производительность
- Простая интеграция с существующим кодом

### Вариант 2: Python с FastAPI
**Преимущества:**
- Автоматическая документация OpenAPI
- Простота разработки
- Асинхронная поддержка

**Рекомендация:** Go с Gin, так как проект уже на Go и это обеспечит лучшую интеграцию.

## 📊 Структура API

### Основные эндпоинты:

```
/api/v1/
├── health          # Статус системы
├── config          # Конфигурация
├── tests           # Управление тестами
├── results         # Результаты
└── proxies         # Управление прокси
```

### Детальная структура:

#### 1. Health & Status
```http
GET  /api/v1/health          # Общий статус системы
GET  /api/v1/status          # Детальный статус
GET  /api/v1/metrics         # Prometheus метрики
```

#### 2. Configuration
```http
GET  /api/v1/config          # Текущая конфигурация
POST /api/v1/config          # Обновление конфигурации
GET  /api/v1/config/defaults # Конфигурация по умолчанию
```

#### 3. Test Management
```http
POST /api/v1/tests           # Запуск нового теста
GET  /api/v1/tests           # Список тестов
GET  /api/v1/tests/{id}      # Статус конкретного теста
DELETE /api/v1/tests/{id}    # Остановка теста
POST /api/v1/tests/{id}/cancel # Отмена теста
```

#### 4. Results
```http
GET  /api/v1/results         # Последние результаты
GET  /api/v1/results/{id}    # Результаты конкретного теста
GET  /api/v1/results/{id}/proxies # Список прокси теста
GET  /api/v1/results/{id}/working # Рабочие прокси
GET  /api/v1/results/{id}/failed # Неуспешные прокси
POST /api/v1/results/{id}/export # Экспорт результатов
```

#### 5. Proxy Management
```http
POST /api/v1/proxies/upload  # Загрузка файла с прокси
GET  /api/v1/proxies         # Список доступных прокси
GET  /api/v1/proxies/{id}    # Информация о прокси
POST /api/v1/proxies/test    # Тестирование конкретной прокси
```

## 🎪 Примеры использования

### Запуск теста:
```http
POST /api/v1/tests
Content-Type: application/json

{
  "name": "nightly-test",
  "proxy_count": 20,
  "timeout": 30,
  "check_method": "ip",
  "ip_check_url": "https://api.ipify.org"
}
```

**Ответ:**
```json
{
  "test_id": "test_12345",
  "status": "started",
  "message": "Test started successfully",
  "started_at": "2025-10-30T05:30:49Z"
}
```

### Получение результатов:
```http
GET /api/v1/results/test_12345
```

**Ответ:**
```json
{
  "test_id": "test_12345",
  "status": "completed",
  "total_proxies": 20,
  "successful": 4,
  "failed": 16,
  "success_rate": 20.0,
  "average_latency": "2.788s",
  "started_at": "2025-10-30T05:30:49Z",
  "completed_at": "2025-10-30T05:31:19Z",
  "working_proxies": [
    {
      "name": "🇳🇱[openproxylist.com] ss-NL",
      "protocol": "shadowsocks",
      "server": "45.87.175.28:8080",
      "latency": "1.108s",
      "rank": 1
    }
  ]
}
```

## 🏗️ Техническая реализация

### Структура проекта:
```
projectx/
├── cmd/
│   └── api/           # API сервер
├── internal/
│   ├── api/           # Обработчики API
│   ├── service/        # Бизнес-логика
│   ├── storage/       # Хранение данных
│   └── models/        # Модели данных
├── pkg/
│   └── proxytestlib/  # Существующий код
└── api/
    └── v1/           # OpenAPI спецификация
```

### Основные компоненты:

#### 1. API Server (cmd/api/main.go)
- Инициализация Gin/Gorilla Mux
- Middleware (логгирование, CORS, аутентификация)
- Маршрутизация

#### 2. Service Layer (internal/service/)
- `TestService` - управление тестами
- `ProxyService` - работа с прокси
- `ResultService` - обработка результатов

#### 3. Storage Layer (internal/storage/)
- `TestStore` - хранение тестов
- `ResultStore` - хранение результатов
- In-memory + файловое хранилище

#### 4. Models (internal/models/)
```go
type Test struct {
    ID          string    `json:"id"`
    Name        string    `json:"name"`
    Status      string    `json:"status"` // pending, running, completed, failed
    ProxyCount  int       `json:"proxy_count"`
    StartedAt   time.Time `json:"started_at"`
    CompletedAt time.Time `json:"completed_at"`
}

type TestResult struct {
    TestID       string        `json:"test_id"`
    TotalProxies int           `json:"total_proxies"`
    Successful   int           `json:"successful"`
    Failed       int           `json:"failed"`
    SuccessRate  float64       `json:"success_rate"`
    AverageLatency time.Duration `json:"average_latency"`
    WorkingProxies []ProxyResult `json:"working_proxies"`
}
```

## 🔧 План реализации

### Этап 1: Базовый API (1-2 дня)
1. Настройка Gin/Gorilla Mux
2. Эндпоинты health и config
3. Базовая структура проекта

### Этап 2: Управление тестами (2-3 дня)
1. Запуск/остановка тестов
2. Асинхронная обработка
3. Хранение состояния

### Этап 3: Результаты и статистика (2-3 дня)
1. Получение результатов
2. Форматирование данных
3. Экспорт результатов

### Этап 4: Дополнительные функции (1-2 дня)
1. Аутентификация
2. Документация OpenAPI
3. Мониторинг и логирование

## 🚀 Быстрый старт

Создам минимальную версию API для демонстрации:

### 1. Базовая структура API сервера
```go
// cmd/api/main.go
package main

import (
	"log"
	"net/http"
	"time"
	
	"github.com/gin-gonic/gin"
)

func main() {
	r := gin.Default()
	
	// Middleware
	r.Use(gin.Logger())
	r.Use(gin.Recovery())
	
	// Health check
	r.GET("/health", func(c *gin.Context) {
		c.JSON(http.StatusOK, gin.H{
			"status":    "healthy",
			"timestamp": time.Now().Unix(),
			"version":   "1.0.0",
		})
	})
	
	// API routes
	api := r.Group("/api/v1")
	{
		api.GET("/status", getStatus)
		api.POST("/tests", startTest)
		api.GET("/tests/:id", getTestStatus)
		api.GET("/results/:id", getResults)
	}
	
	log.Println("API server starting on :8080")
	log.Fatal(r.Run(":8080"))
}
```

### 2. Обработчики API
```go
// internal/api/handlers.go
package api

import (
	"net/http"
	
	"github.com/gin-gonic/gin"
)

func getStatus(c *gin.Context) {
	c.JSON(http.StatusOK, gin.H{
		"system": "proxy-test-api",
		"status": "running",
		"uptime": "0s", // TODO: calculate
	})
}

func startTest(c *gin.Context) {
	// TODO: implement test starting logic
	c.JSON(http.StatusOK, gin.H{
		"test_id": "test_123",
		"status":  "started",
		"message": "Test started successfully",
	})
}

func getTestStatus(c *gin.Context) {
	testID := c.Param("id")
	// TODO: get test status from storage
	c.JSON(http.StatusOK, gin.H{
		"test_id": testID,
		"status":  "running",
		"progress": "50%",
	})
}

func getResults(c *gin.Context) {
	testID := c.Param("id")
	// TODO: get results from storage
	c.JSON(http.StatusOK, gin.H{
		"test_id": testID,
		"results": "pending",
	})
}
```

## 📈 Преимущества предложенной архитектуры

### Для разработки:
- **Модульность:** Четкое разделение ответственности
- **Масштабируемость:** Легко добавлять новые функции
- **Тестируемость:** Изолированные компоненты

### Для пользователей:
- **Простой интерфейс:** REST API с JSON
- **Гибкость:** Разные клиенты (CLI, Web, Mobile)
- **Документация:** Автоматическая генерация OpenAPI

### Для операций:
- **Мониторинг:** Метрики и health checks
- **Логирование:** Детальные логи операций
- **Надежность:** Обработка ошибок и retry логика

## 🎯 Рекомендации по реализации

1. **Начать с минимальной версии** и постепенно добавлять функции
2. **Использовать существующий код** `proxytestlib` как библиотеку
3. **Реализовать асинхронную обработку** для длительных операций
4. **Добавить документацию** с самого начала
5. **Реализовать базовую аутентификацию** для безопасности

Эта архитектура обеспечит надежную основу для API сервиса тестирования прокси с возможностью дальнейшего расширения.