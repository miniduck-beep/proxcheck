# 🚀 Руководство по использованию Proxy Test API

## 📋 Содержание
1. [Быстрый старт](#быстрый-старт)
2. [Запуск сервера](#запуск-сервера)
3. [Использование клиента](#использование-клиента)
4. [Примеры команд](#примеры-команд)
5. [API эндпоинты](#api-эндпоинты)
6. [Конфигурация](#конфигурация)
7. [Устранение проблем](#устранение-проблем)

## 🚀 Быстрый старт

### 1. Запуск сервера (в одном терминале)
```bash
cd /Users/t/projectx/cmd/api

# Запуск с настраиваемым портом
./api_server -port 9090 -data-dir ./data

# Или простой запуск
go run improved_api.go -port 9090
```

### 2. Проверка работы (в другом терминале)
```bash
cd /Users/t/projectx/cmd/api

# Быстрая проверка
./api_client -action health

# Полный демо-сценарий
./api_client -action demo
```

## 🖥️ Запуск сервера

### Доступные параметры сервера

```bash
# Показать справку
./api_server --help

# Запуск с разными портами
./api_server -port 8080                    # Порт 8080
./api_server -port 9090                    # Порт 9090
./api_server -port 3000                    # Порт 3000

# С указанием директории данных
./api_server -port 9090 -data-dir ./data

# С подробным выводом
./api_server -port 9090 -verbose
```

### Примеры запуска сервера

```bash
# Стандартный запуск
./api_server -port 9090

# Запуск с сохранением данных
./api_server -port 9090 -data-dir /tmp/proxy-test-data

# Проверка занятости порта
lsof -i :9090

# Если порт занят, освободить его
kill -9 $(lsof -ti:9090)
```

## 📱 Использование клиента

### Доступные действия (actions)

- `health` - Проверка состояния сервера
- `status` - Статус системы
- `config` - Конфигурация
- `list` - Список тестов
- `test` - Запуск теста
- `results` - Получение результатов
- `export` - Экспорт результатов
- `demo` - Полный демо-сценарий

### Параметры клиента

```bash
# Основные параметры
-port 9090              # Порт сервера (по умолчанию: 9090)
-host localhost         # Хост сервера
-action demo           # Действие
-verbose               # Подробный вывод

# Для тестирования
-name "мой-тест"       # Имя теста
-count 20              # Количество прокси
-config "путь/к/файлу" # Файл конфигураций
-start-port 20000      # Стартовый порт
-test-id "test_123"    # ID теста
```

## 📋 Примеры команд

### 1. Проверка состояния

```bash
# Проверка здоровья
./api_client -action health

# Статус системы
./api_client -action status

# Конфигурация
./api_client -action config
```

### 2. Управление тестами

```bash
# Список всех тестов
./api_client -action list

# Запуск теста с параметрами
./api_client -action test -name "ночной-тест" -count 30

# Запуск с кастомным файлом
./api_client -action test -name "тест-с-файлом" -config "/путь/к/deduplicated.json"

# С указанием порта
./api_client -action test -start-port 25000
```

### 3. Получение результатов

```bash
# Получить результаты (нужен test-id)
./api_client -action results -test-id "test_20251030123045"

# Экспорт в файл
./api_client -action export -test-id "test_20251030123045"

# Если не знаете ID теста, сначала посмотрите список
./api_client -action list
```

### 4. Демо-сценарий

```bash
# Полный демо-сценарий
./api_client -action demo

# Демо с параметрами
./api_client -action demo -name "демо-тест" -count 15

# Демо с подробным выводом
./api_client -action demo -verbose
```

## 🌐 API эндпоинты

### Базовые эндпоинты

```http
GET  http://localhost:9090/health
GET  http://localhost:9090/api/v1/status
GET  http://localhost:9090/api/v1/config
```

### Управление тестами

```http
POST http://localhost:9090/api/v1/tests
GET  http://localhost:9090/api/v1/tests/
GET  http://localhost:9090/api/v1/tests/{test_id}
```

### Результаты

```http
GET  http://localhost:9090/api/v1/results/{test_id}
GET  http://localhost:9090/api/v1/results/{test_id}/working
GET  http://localhost:9090/api/v1/results/{test_id}/export
```

### Примеры запросов через curl

```bash
# Health check
curl http://localhost:9090/health

# Статус системы
curl http://localhost:9090/api/v1/status

# Запуск теста
curl -X POST http://localhost:9090/api/v1/tests \
  -H "Content-Type: application/json" \
  -d '{
    "name": "curl-test",
    "proxy_count": 10,
    "config_file": "/Users/t/zapret/test_xray_finish/deduplicated.json"
  }'

# Получение результатов
curl http://localhost:9090/api/v1/results/test_20251030123045
```

## ⚙️ Конфигурация

### Файлы конфигурации

Система поддерживает различные файлы конфигураций:

```bash
# Стандартный файл (автоматически используется)
/Users/t/zapret/test_xray_finish/deduplicated.json

# Кастомный файл (указывается параметром)
-config "/путь/к/вашему/файлу.json"
```

### Параметры тестирования

```json
{
  "name": "мой-тест",           // Имя теста
  "proxy_count": 20,           // Количество прокси (1-100)
  "timeout": 30,               // Таймаут в секундах (1-300)
  "config_file": "путь/файл",   // Файл конфигураций
  "start_port": 20000          // Стартовый порт Xray
}
```

### Директории данных

```bash
# По умолчанию
/tmp/proxy-test-api

# Кастомная директория
-data-dir ./my-data

# Содержимое директории:
📁 data/
  📄 proxies_test_20251030123045.txt    # Экспортированные результаты
  📄 test_metadata.json                 # Метаданные тестов
```

## 🛠️ Устранение проблем

### Сервер не запускается

```bash
# Порт занят
lsof -i :9090
kill -9 $(lsof -ti:9090)

# Недостаточно прав
sudo ./api_server -port 80

# Ошибки зависимостей
cd /Users/t/projectx/cmd/api
go mod tidy
go run improved_api.go
```

### Клиент не подключается

```bash
# Проверка сети
ping localhost

# Проверка порта
telnet localhost 9090

# Проверка firewalls
sudo ufw status

# Разные хосты
./api_client -host 127.0.0.1 -port 9090
./api_client -host 0.0.0.0 -port 9090
```

### Ошибки тестирования

```bash
# Файл конфигураций не найден
ls -la /Users/t/zapret/test_xray_finish/deduplicated.json

# Недостаточно прав
chmod +r /путь/к/файлу.json

# Проверка формата файла
file /путь/к/файлу.json
```

### Мониторинг и логи

```bash
# Логи сервера (выводятся в консоль)
[API] Test test_20251030123045 started
[API] Test test_20251030123045 completed in 5.2s

# Проверка активных тестов
./api_client -action status

# Просмотр списка тестов
./api_client -action list
```

## 📊 Пример вывода

### Успешный запуск сервера

```bash
🚀 Proxy Test API server starting on :9090
🔍 Health check: http://localhost:9090/health
📊 System status: http://localhost:9090/api/v1/status
💾 Data directory: /tmp/proxy-test-api
⚙️  Use --help for command line options
```

### Успешный демо-сценарий

```bash
🎯 Demo Scenario
====================

1. Health Check
---------------
✅ API is healthy
Status: healthy
Version: 1.0.0

2. System Status
---------------
✅ System status retrieved
Active Tests: 0
Total Tests: 5

3. Start Test
-------------
✅ Test started successfully
Test ID: test_20251030123045

4. Results
----------
📊 Test Results:
Total Proxies: 20
Successful: 4
Success Rate: 20.0%

🏆 Working Proxies:
1. 🇳🇱[openproxylist.com] ss-NL (shadowsocks) - 1.108s
2. 🇬🇧GB-141.98.101.178-3885 (shadowsocks) - 1.256s

🎉 Demo Completed!
```

## 🔄 Интеграция с реальным тестированием

### Текущее состояние
- ✅ API сервер готов
- ✅ Клиент с интерфейсом командной строки
- ✅ Управление тестами
- ✅ Получение результатов
- ⏳ Заглушки вместо реального тестирования

### Следующие шаги
1. **Интеграция с proxytestlib** - замена заглушек
2. **Реальное тестирование прокси** - HTTP запросы
3. **База данных** - постоянное хранение
4. **Веб-интерфейс** - графическое управление

## 💡 Советы

1. **Всегда сначала запускайте сервер**, потом клиент
2. **Используйте разные имена тестов** для отслеживания
3. **Проверяйте наличие файлов конфигураций** перед тестированием
4. **Используйте параметр `-verbose`** для отладки
5. **Экспортируйте результаты** для анализа

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервера
2. Используйте `-verbose` для подробного вывода
3. Убедитесь, что порт не занят
4. Проверьте доступность файлов конфигураций

---

**🚀 Готово к использованию! Начните с `./api_client -action demo`**